using Cinemachine;
using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class PlayerSwitch : MonoBehaviour
{
    /*
    private void Update()
    {
        switchCamersFromPlayer();
    }
    CinemachineVirtualCamera[] listcamers = new CinemachineVirtualCamera[2];
    public void switchCamersFromPlayer()
    {
        foreach (var camer in listcamers)
        {
            camer.Follow = transform;
        }
    }
    */
}
